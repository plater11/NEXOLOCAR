import { admin,input,clean,date,dedupe,report,upsertBatches } from "./lib.mjs";
const source=await input("clientes");const normalized=source.map((r,i)=>({codigo:clean(r.id||r.codigo),nombre:clean([r.nombre,r.apellidos].filter(Boolean).join(" "))||"SIN NOMBRE",telefono:clean(r.contacto||r.telefono)||null,direccion:clean(r.direccion)||null,correo:clean(r.correo)||null,fecha_nacimiento:date(r.fechaCumpleanos||r.fecha_nacimiento),observaciones:clean(r.comentarios||r.observaciones)||null,estado:clean(r.estado||"ACTIVO").toUpperCase(),legacy_id:clean(r.id||r.codigo),legacy_row:i+2,legacy_source:"SHEETS"}));const unique=dedupe(normalized,r=>r.codigo);const result=await upsertBatches(admin(),"clientes",unique.rows,"codigo");await report("clientes",{source:source.length,valid:unique.rows.length,duplicates:unique.duplicates,...result});if(result.errors.length)process.exitCode=1;
